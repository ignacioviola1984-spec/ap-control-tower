"""Opcion 1: Probar con mis facturas (carga manual + import Gmail).

Placeholder de scaffolding (commit 1). La carga manual, el procesamiento
session-only y la importacion Gmail se implementan en los commits siguientes.
"""

from __future__ import annotations

import streamlit as st


def render() -> None:
    st.markdown("## Probar con mis facturas")
    st.info("Carga de PDFs e importación desde Gmail — en preparación.")
