# Dependencias de la API interna OPCIONAL (Fase 4).
# NO se instalan en la imagen de la demo (Dockerfile usa solo requirements.txt).
# Instalar aparte para levantar la API:
#   pip install -r requirements-api.txt
#   uvicorn ap_control_tower.api.main:app --port 8000
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
# httpx lo usa el TestClient de FastAPI en evals/test_api.py
httpx>=0.27
